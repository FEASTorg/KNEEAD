# See https://pre-commit.com for more info
# Install: pip install pre-commit && pre-commit install

repos:
  - repo: local
    hooks:
      - id: update-indexes
        name: Update calculator and page indexes
        entry: python scripts/generate_indexes.py
        language: python
        pass_filenames: false
        always_run: true
